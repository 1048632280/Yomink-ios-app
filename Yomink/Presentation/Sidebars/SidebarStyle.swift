import SwiftUI

enum SidebarStyle {
    static let background = Color(.darkGray)
    static let rowBackground = Color(.gray)
    static let selectedRowBackground = Color(.systemGray2)
    static let primaryText = Color.white
    static let secondaryText = Color(.systemGray5)
    static let icon = Color.white
}
