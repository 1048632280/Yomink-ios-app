import SwiftUI

enum SidebarStyle {
    static let background = Color(
        red: 28.0 / 255.0,
        green: 28.0 / 255.0,
        blue: 30.0 / 255.0
    )
    static let rowBackground = Color(
        red: 44.0 / 255.0,
        green: 44.0 / 255.0,
        blue: 46.0 / 255.0
    )
    static let separator = Color(
        red: 51.0 / 255.0,
        green: 51.0 / 255.0,
        blue: 51.0 / 255.0
    )
    static let primaryText = Color.white
    static let secondaryText = Color(.systemGray3)
    static let itemHeight: CGFloat = 54
    static let groupHeaderHeight: CGFloat = 34
}
