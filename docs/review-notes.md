# 项目整体 Review 记录

Review 日期：2026-05-30

范围：当前 `Yomink-ios-app` 仓库整体审查，覆盖源码结构、导入/阅读/删除链路、数据层、构建配置、测试与 CI 风险。初始 review 阶段除本文件外未修改业务代码；后续修复持续更新本文件状态。

状态约定：

- 待处理：已确认风险，尚未修复。
- 待验证：需要 macOS/Xcode、真机或专项样例进一步验证。
- 已修复：后续修复完成并验证后更新。
- 暂不处理：明确接受风险后更新。

## 必须修

| 问题编号 | 模块 | 文件行号 | 严重程度 | 问题 | 建议处理方式 | 当前状态 |
| --- | --- | --- | --- | --- | --- | --- |
| R-001 | Reader / Chapter | `Yomink/Data/ChapterIndexer/ChapterIndexer.swift:4`, `Yomink/Presentation/Reader/ReaderHostView.swift:6145`, `Yomink/Presentation/Reader/ReaderHostView.swift:6173`, `Yomink/Presentation/Reader/ReaderHostView.swift:6268` | 必须修 | 章节识别成功后不再限制单章大小，阅读器每次打开页面会读取整个章节并对整个章节分页。若 TXT 只有 1 个超大章节或少数大章节，可能一次读取/排版接近整本书，违背 30MB TXT 与 50MB 内存目标，也会导致主阅读链路卡顿。 | 已在 `ChapterIndexer` 对 regex 章节增加 128KB 最大块兜底切分，并新增 `ChapterIndexerTests` 覆盖大章与 UTF-8 边界；用户已测试通过。 | 已修复 |
| R-002 | Import | `Yomink/Domain/Services/ImportService.swift:92`, `Yomink/Domain/Services/ImportService.swift:159`, `Yomink/Domain/Services/ImportService.swift:166`, `Yomink/Domain/Services/ImportService.swift:167`, `Yomink/Domain/Services/ImportService.swift:187`, `Yomink/Domain/Services/ImportService.swift:236` | 必须修 | 导入与重复检测会多次整文件读取/解码：重复检测先 hash 一次，确认导入时又读取、解码、生成 UTF-8 Data、计算 hash、章节扫描。30MB 文件在峰值可能同时存在原始 Data、String、UTF-8 Data 和 hash Data，内存目标风险很高。 | 已新增 `importBookCheckingDuplicate`，确认导入时复用同一次准备导入产生的 contentHash 做去重，避免源文件重复读解码；hash 复用 UTF-8 Data，已存 canonical 文件 hash 直接用 Data；用户已测试通过。 | 已修复 |
| R-003 | Library / Storage / Data | `Yomink/Presentation/Library/LibraryView.swift:1267`, `Yomink/Presentation/Library/LibraryView.swift:1282`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:1878`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:1881`, `Yomink/Data/Database/GRDBLibraryRepository.swift:860`, `Yomink/Data/FileStore/AppFileStore.swift:100` | 必须修 | 删除图书时先删数据库再删文件，且删除逻辑散落在书架和存储管理两处。若文件删除失败，数据库记录已消失，后续无法通过 UI 找回或清理该书目录，形成孤儿文件。 | 已将书架批量删除和存储页单本删除改为先删文件、成功后再删 DB；文件失败时 DB 记录保留，可从 UI 重试；用户已测试通过。 | 已修复 |
| R-004 | Tests / CI | `Yomink.xcodeproj/project.pbxproj:258`, `Yomink.xcodeproj/xcshareddata/xcschemes/Yomink.xcscheme:11`, `.github/workflows/build-unsigned-ipa.yml:37` | 必须修 | 项目只有 App target，scheme 的 `Testables` 为空，CI 只归档 unsigned IPA，没有 `xcodebuild test`、单元测试或类型/静态检查门禁。核心的编码识别、章节偏移、过滤映射、数据库迁移都没有自动回归保护。 | 已新增 `YominkTests` XCTest target、把它接入 shared scheme，并在 CI archive 前运行 `xcodebuild test`；首批测试覆盖 `ChapterIndexer` 大章切分；用户已测试通过。后续继续补 `TXTTextDecoder`、过滤映射、迁移、导入去重和删除清理测试。 | 已修复 |

## 建议修

| 问题编号 | 模块 | 文件行号 | 严重程度 | 问题 | 建议处理方式 | 当前状态 |
| --- | --- | --- | --- | --- | --- | --- |
| R-005 | Reader / Search | `Yomink/Presentation/Reader/ReaderMoreViewControllers.swift:1115`, `Yomink/Presentation/Reader/ReaderMoreViewControllers.swift:1243`, `Yomink/Presentation/Reader/ReaderMoreViewControllers.swift:1320`, `Yomink/Presentation/Reader/ReaderMoreViewControllers.swift:1460` | 建议修 | 正文搜索当前按章节线性扫描并读取整章，只保留单章缓存。大章节或多次搜索会重复 I/O、过滤和字符串扫描；若配合 R-001 的大章节情况，搜索体验和内存都不稳定。 | 短期按固定 byte chunk 扫描并显示进度/取消；中期落地 FTS5 或导入阶段建立章节级搜索索引；复用统一的章节文本读取服务。 | 待处理 |
| R-006 | Reader / Data Access | `Yomink/Presentation/Reader/ReaderHostView.swift:6268`, `Yomink/Presentation/Reader/ReaderMoreViewControllers.swift:323`, `Yomink/Presentation/Reader/ReaderMoreViewControllers.swift:1460` | 建议修 | 章节读取逻辑重复三份，错误行为不一致：阅读器遇到无效 UTF-8 会抛错，详情/搜索路径会把无效内容吞成空字符串。后续修改文件格式或偏移策略时容易漏改。 | 抽出 `ChapterTextReader` 或 `ReaderContentStore`，统一读取、UTF-8 校验、错误上报和缓存策略。 | 待处理 |
| R-007 | Presentation Structure | `Yomink/Presentation/Reader/ReaderHostView.swift:13`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:5`, `Yomink/Presentation/Library/LibraryView.swift:5`, `Yomink/Presentation/Reader/ReaderMoreViewControllers.swift:801` | 建议修 | Presentation 层文件明显过大并混合多种职责：阅读器控制器、分页器、设置面板、自动阅读、目录、搜索、存储页等集中在少数文件中。当前单文件最大约 258KB，后续行为修复会变得高风险。 | 按职责拆分：Reader controller、Paginator、AutoReadController、ReaderSettingsPanel、ReaderContents、ReaderSearch、StorageManagement 等。拆分时先补测试，避免纯搬迁引入行为差异。 | 待处理 |
| R-008 | Export / Storage | `Yomink/Presentation/Library/LibraryView.swift:1302`, `Yomink/Presentation/Library/LibraryView.swift:1351`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:2877`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:2913` | 建议修 | 导出目录、文件名清理、复制逻辑在书架批量导出和存储页单本导出中重复。批量路径处理重名，单本路径依赖先清空临时目录；后续改导出规则容易不一致。 | 抽出 `BookExportService`，统一临时目录生命周期、文件名去重、错误处理和分享完成后的清理。 | 待处理 |
| R-009 | Reader / Progress | `Yomink/Data/Database/GRDBLibraryRepository.swift:749`, `Yomink/Presentation/Reader/ReaderHostView.swift:1756`, `Yomink/Presentation/Reader/ReaderHostView.swift:2571`, `Yomink/Presentation/Reader/ReaderHostView.swift:2737` | 建议修 | 打开书籍时立即写 `lastReadAt` 与足迹，但真实阅读进度依赖后续节流保存。若用户快速打开又退出，足迹可能记录旧 offset，和本次打开位置/展示状态不完全一致。 | 在首屏定位完成后保存一次准确 progress/history，或将 `markBookOpened` 接收当前定位结果；退出时确保立即保存成功失败可观测。 | 待验证 |
| R-010 | Database / Settings | `Yomink/Data/Database/GRDBLibraryRepository.swift:612`, `Yomink/Data/Database/GRDBLibraryRepository.swift:657`, `Yomink/Data/Database/GRDBLibraryRepository.swift:703` | 建议修 | App 设置 JSON 解码失败会静默回退默认值，用户可能无感丢失书架排序、抽书状态或阅读设置；也没有清理损坏配置或记录诊断信息。 | 解码失败时写 OSLog，并可选择重置对应 key 或保留错误状态供 UI 提示；为设置 schema 演进增加兼容测试。 | 待处理 |
| R-011 | Build / Local Verification | `.github/workflows/build-unsigned-ipa.yml:28`, `.github/workflows/build-unsigned-ipa.yml:37`, `README.md:19` | 建议修 | 当前仓库在 Windows 本地无法运行 `xcodebuild` 或 Swift 编译检查，只能依赖 GitHub Actions；而 Actions 只做 Release archive，反馈慢且缺少 Debug/simulator 编译覆盖。 | CI 增加 Debug simulator build/test；在 README 写清本地验证路径；必要时提供 `make ci-check` 或脚本封装 Xcode 命令。 | 待处理 |
| R-016 | App / Bootstrap | `Yomink/App/YominkApp.swift:12`, `Yomink/App/AppEnvironment.swift:22`, `Yomink/App/AppEnvironment.swift:43`, `Yomink/Presentation/Library/LibraryView.swift:738` | 建议修 | App 启动时同步执行文件目录创建、数据库打开和 migration，并且 bootstrap 失败后只有终态 `.failed(String)`，UI 只能展示错误文本，没有重试入口。数据库迁移变重或偶发文件系统失败时，会阻塞首屏或要求用户重启 App。 | 将 `AppEnvironment` 调整为显式 `.bootstrapping / .ready / .failed` 状态，启动后异步初始化；失败态保留原始错误和重试方法，UI 提供“重试初始化”。迁移耗时也应进入可观测日志。 | 待处理 |
| R-017 | App / Runtime Bridge | `Yomink/App/YominkApp.swift:8`, `Yomink/App/YominkApp.swift:11`, `Yomink/Presentation/Reader/HostingControllerHomeIndicatorBridge.swift:25`, `Yomink/Presentation/Reader/ReaderHostView.swift:482` | 建议修 | App 入口直接安装 Reader 相关的 `UIHostingController` runtime swizzle，职责边界不清：全局启动逻辑依赖 Presentation/Reader 内部 workaround，且该 bridge 会遍历并替换所有 hosting controller 子类方法。 | 将 bridge 移到 App/Infrastructure 并命名为全局 UIKit 兼容层，或限制为 Reader host 使用的自定义 hosting/安装路径；补充最小 UI 回归验证，确保 home indicator 与边缘手势行为没有被其他 SwiftUI 页面意外影响。 | 待处理 |
| R-018 | App / Assets | `Yomink/App/Assets.xcassets/Contents.json:1`, `Yomink.xcodeproj/project.pbxproj:498`, `Yomink.xcodeproj/project.pbxproj:525` | 建议修 | Assets 目录目前只有 `AccentColor`，target build settings 也没有 `ASSETCATALOG_COMPILER_APPICON_NAME`。这不会妨碍早期代码审查，但安装包会缺少正式 App 图标，后续签名分发或 App Store 验证存在风险。 | 增加 `AppIcon.appiconset` 并在 Debug/Release target build settings 中声明 app icon；同时保留当前 AccentColor。若暂不准备分发，也应在 README/计划中标注为发布前阻塞项。 | 待处理 |
| R-021 | Domain / Layering | `Yomink/Domain/Services/ImportService.swift:1`, `Yomink/Domain/Services/ImportService.swift:44`, `Yomink/Domain/Services/ImportService.swift:46`, `Yomink/Domain/Services/ImportService.swift:47`, `Yomink/Domain/Services/ImportService.swift:130`, `Yomink/Domain/Services/ImportService.swift:161`, `Yomink/Domain/Services/ImportService.swift:170`, `Yomink/Domain/Services/LibraryRepository.swift:57` | 建议修 | Domain 层并不纯粹：`ImportService` 直接依赖 `CryptoKit`、`AppFileStore`、`TXTTextDecoder`、`ChapterIndexer`、`FileManager`、`Data(contentsOf:)` 和安全作用域 URL，仓储协议注释也要求调用方知道 `AppFileStore` 清理细节。导入、文件系统、编码解码、章节索引和本地化错误文案混在 Domain，会让职责边界继续向内泄漏。 | 将导入编排迁到 Application/UseCases 或 Infrastructure；Domain 只保留实体、值对象、领域错误和协议抽象。文件读写、hash、解码、章节索引、清理补偿由专门服务注入，删除契约也改成业务语义而不是引用 `AppFileStore` 实现细节。 | 待处理 |
| R-022 | Domain / Repository | `Yomink/Domain/Services/LibraryRepository.swift:3`, `Yomink/Domain/Services/LibraryRepository.swift:4`, `Yomink/Domain/Services/LibraryRepository.swift:10`, `Yomink/Domain/Services/LibraryRepository.swift:22`, `Yomink/Domain/Services/LibraryRepository.swift:31`, `Yomink/Domain/Services/LibraryRepository.swift:43`, `Yomink/Domain/Services/LibraryRepository.swift:48`, `Yomink/Domain/Services/LibraryRepository.swift:55` | 建议修 | `LibraryRepository` 是一个过宽的总线协议，书籍列表、搜索历史、分组、章节、书签、过滤规则、阅读进度、设置、抽书状态、导入和删除都塞进同一个接口。任何 preview fake 或真实仓储都被迫实现全量能力，后续拆模块、测试替身和权限边界都会变重。 | 按用例拆成更小的协议，例如 `BookRepository`、`GroupRepository`、`ReadingProgressRepository`、`BookmarkRepository`、`FilterRuleRepository`、`LibrarySettingsRepository`、`ImportBookRepository`；Presentation 只依赖当前页面实际需要的组合。 | 待处理 |
| R-023 | Domain / Reader Settings | `Yomink/Domain/Models/ReadingProgress.swift:3`, `Yomink/Domain/Models/ReadingProgress.swift:15`, `Yomink/Domain/Models/ReadingProgress.swift:29`, `Yomink/Domain/Models/ReadingProgress.swift:38`, `Yomink/Domain/Models/ReadingProgress.swift:46`, `Yomink/Domain/Models/ReadingProgress.swift:67`, `Yomink/Domain/Models/ReadingProgress.swift:258`, `Yomink/Domain/Models/ReadingProgress.swift:289`, `Yomink/Domain/Models/ReadingProgress.swift:296` | 建议修 | `ReadingProgress.swift` 同时承载阅读进度、章节、书签、过滤规则和完整 `ReaderSettings`；其中 `ReaderSettings` 又包含字体、边距、widget 显示、home indicator、status bar、边缘返回、九宫格触控区等 UI/布局细节。文件名和职责已经失焦，Domain 也被 Presentation 配置污染。 | 拆出 `Chapter.swift`、`Bookmark.swift`、`TextFilterRule.swift`、`ReaderSettings.swift` 等文件；进一步把 `ReaderLayoutSettings`、`ReaderChromeSettings`、`TouchAreaMap` 等 UI 配置迁到 Presentation 或 Application settings，只在 Domain 保留真正跨层稳定的阅读偏好。 | 待处理 |
| R-024 | Domain / Model Invariants | `Yomink/Domain/Models/Book.swift:9`, `Yomink/Domain/Models/Book.swift:11`, `Yomink/Domain/Models/Book.swift:15`, `Yomink/Domain/Models/ReadingProgress.swift:6`, `Yomink/Domain/Models/ReadingProgress.swift:7`, `Yomink/Domain/Models/ReadingProgress.swift:19`, `Yomink/Domain/Models/ReadingProgress.swift:20`, `Yomink/Domain/Models/ReadingProgress.swift:33`, `Yomink/Domain/Models/ImportedBookDraft.swift:23`, `Yomink/Domain/Models/ImportedBookDraft.swift:25` | 建议修 | 多个 Domain 模型是可变属性集合，缺少领域不变量：`fileSize`、`wordCount`、`progressPercentage`、`globalProgress`、`chapterOffset`、章节起止 offset、书签 offset 都可以构造出负数、超过 1 的进度或 `endOffset < startOffset` 的状态。当前部分 Data/UI 侧会 clamp，但领域层本身仍能传播非法状态，边界条件 bug 容易在新调用点复现。 | 给关键模型增加验证/归一化入口，例如 failable/throwing initializer、`normalized()` 或专门值对象；把进度限制在 0...1，offset 和大小限制为非负，章节范围要求 `endOffset >= startOffset`。同时补充模型或仓储映射测试，保证非法持久化数据不会进入 UI。 | 待处理 |
| R-027 | Data / Repository Structure | `Yomink/Data/Database/GRDBLibraryRepository.swift:5`, `Yomink/Data/Database/GRDBLibraryRepository.swift:40`, `Yomink/Data/Database/GRDBLibraryRepository.swift:160`, `Yomink/Data/Database/GRDBLibraryRepository.swift:321`, `Yomink/Data/Database/GRDBLibraryRepository.swift:402`, `Yomink/Data/Database/GRDBLibraryRepository.swift:612`, `Yomink/Data/Database/GRDBLibraryRepository.swift:792`, `Yomink/Data/Database/GRDBLibraryRepository.swift:902` | 建议修 | `GRDBLibraryRepository` 单文件承载了书籍查询、搜索历史、分组、章节、书签、过滤规则、阅读历史、设置 JSON、导入插入、删除和 Row 映射，已经超过千行。Data 层职责集中在一个类型里，后续修 SQL、迁移或模型映射时很容易产生横向影响。 | 先按表/用例拆成内部 repository 或 DAO，例如 `BookStore`、`GroupStore`、`ReaderStateStore`、`SettingsStore`、`ImportStore`；共享 DB writer 和公共映射工具即可。配合 R-022 的协议拆分，让 Data 实现与业务用例边界一致。 | 待处理 |
| R-028 | Data / SQL Duplication | `Yomink/Data/Database/GRDBLibraryRepository.swift:21`, `Yomink/Data/Database/GRDBLibraryRepository.swift:45`, `Yomink/Data/Database/GRDBLibraryRepository.swift:89`, `Yomink/Data/Database/GRDBLibraryRepository.swift:292`, `Yomink/Data/Database/GRDBLibraryRepository.swift:563`, `Yomink/Data/Database/GRDBLibraryRepository.swift:893` | 建议修 | 查询书籍列表、搜索、按 hash 查找、更新后回读、历史记录都手写 `books.* + reading_progress` 片段和排序逻辑，`LIKE` escape 也散在仓储私有方法里。字段或排序规则变更时需要同步多段 SQL，容易出现某个入口漏加字段、漏 clamp 或排序不一致。 | 抽出 `BookQuery`/`BookRowMapper`/SQL builder 小工具，统一 select 列、progress join、scope where、sort order 和 keyword escape；为每个入口补最小查询快照或行为测试。 | 待处理 |
| R-029 | Data / Database Constraints | `Yomink/Data/Database/AppDatabase.swift:62`, `Yomink/Data/Database/AppDatabase.swift:64`, `Yomink/Data/Database/AppDatabase.swift:89`, `Yomink/Data/Database/AppDatabase.swift:90`, `Yomink/Data/Database/AppDatabase.swift:102`, `Yomink/Data/Database/AppDatabase.swift:103`, `Yomink/Data/Database/AppDatabase.swift:123`, `Yomink/Data/Database/AppDatabase.swift:156`, `Yomink/Data/Database/GRDBLibraryRepository.swift:534`, `Yomink/Data/Database/GRDBLibraryRepository.swift:818` | 建议修 | SQLite schema 缺少关键 `CHECK`/唯一约束：文件大小、字数、进度、offset、章节范围、过滤规则 source 都只靠调用方自觉；`filter_rules.bookId` 还允许 NULL。导入或未来迁移一旦写入非法值，读取层会部分丢行或把非法状态继续传给 UI。 | 增加向后兼容 migration：为新表或重建表补 `CHECK(globalProgress BETWEEN 0 AND 1)`、`offset >= 0`、`endOffset >= startOffset`、`fileSize >= 0`、`wordCount >= 0`、`filter_rules.bookId NOT NULL`，并视需求增加 `(bookId, source)` 唯一约束。写入前也保留应用层验证，测试非法数据迁移路径。 | 待处理 |
| R-030 | Data / Concurrency | `Yomink/Data/Database/DatabaseDateFormatter.swift:3`, `Yomink/Data/Database/DatabaseDateFormatter.swift:4`, `Yomink/Data/Database/DatabaseDateFormatter.swift:15`, `Yomink/Data/Database/DatabaseDateFormatter.swift:19`, `Yomink/Data/Database/GRDBLibraryRepository.swift:137`, `Yomink/Data/Database/GRDBLibraryRepository.swift:553`, `Yomink/Data/Database/GRDBLibraryRepository.swift:914`, `Yomink/Data/Database/GRDBLibraryRepository.swift:1083` | 建议修 | `DatabaseDateFormatter` 用一个静态 `ISO8601DateFormatter` 同时服务多个 async repository 调用。`ISO8601DateFormatter` 不是明确的 Sendable/线程安全值，Data 层又大量在 GRDB read/write 闭包中并发格式化和解析日期，存在偶发解析/格式化竞态风险。 | 已将 `DatabaseDateFormatter` 改为每次调用创建局部 `ISO8601DateFormatter`，避免多个 async 仓储调用共享同一个 formatter 实例；后续可再补并发格式化测试。 | 待验证 |
| R-031 | Data / Filter Rules | `Yomink/Data/Database/GRDBLibraryRepository.swift:418`, `Yomink/Data/Database/GRDBLibraryRepository.swift:431`, `Yomink/Data/Database/GRDBLibraryRepository.swift:456`, `Yomink/Data/Database/GRDBLibraryRepository.swift:461`, `Yomink/Data/Database/GRDBLibraryRepository.swift:464`, `Yomink/Presentation/Reader/ReaderMoreViewControllers.swift:711`, `Yomink/Presentation/Reader/ReaderMoreViewControllers.swift:744` | 建议修 | 过滤规则写入的空 source 行为不一致：`createFilterRule` 遇到空 source 会返回一个未持久化的 `TextFilterRule`，而 `updateFilterRule` 会把空 source 写入数据库；当前 UI 先做了 guard，但仓储契约本身仍允许其他调用点制造不可见或无效规则。 | 已新增 `LibraryRepositoryError.emptyFilterSource`，真实仓储和 preview 仓储的 create/update 都统一拒绝空 source；找不到规则也改为统一仓储错误。数据库 CHECK 仍留给 R-029 处理。 | 待验证 |
| R-034 | Presentation / Library State | `Yomink/Presentation/Library/LibraryView.swift:748`, `Yomink/Presentation/Library/LibraryView.swift:759`, `Yomink/Presentation/Library/LibraryView.swift:1132`, `Yomink/Presentation/Library/LibraryView.swift:1159`, `Yomink/Presentation/Library/LibraryView.swift:1167`, `Yomink/Presentation/Library/LibraryView.swift:1177`, `Yomink/Presentation/Library/LibraryView.swift:1179`, `Yomink/Presentation/Library/LibraryView.swift:1256`, `Yomink/Presentation/Library/LibraryView.swift:1280` | 建议修 | 书架 `loadBooks`/`bootstrap` 没有统一的任务取消或 generation 校验。快速切换分组、排序、导入后刷新、删除后刷新时会启动多个并发加载，旧请求后完成就可能覆盖新作用域或新排序下的 `books/allBooks/groups`，造成列表闪回、选择集被错误裁剪或当前分组展示过期数据。 | 已在 `LibraryViewModel` 增加 `loadGeneration`，`bootstrap`/`loadBooks` 回写前校验 generation，旧请求完成后不会覆盖最新书架状态；未引入额外加载队列。 | 待验证 |
| R-035 | Presentation / Settings State | `Yomink/Presentation/Sidebars/LibrarySidebarView.swift:174`, `Yomink/Presentation/Sidebars/LibrarySidebarView.swift:184`, `Yomink/Presentation/Sidebars/LibrarySidebarView.swift:194`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:1687`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:1755`, `Yomink/Presentation/Library/LibraryView.swift:304`, `Yomink/Presentation/Library/LibraryView.swift:423`, `Yomink/Presentation/Library/LibraryView.swift:1385` | 建议修 | 书架设置状态和持久化入口分散在 Sidebar、设置页和 `LibraryViewModel` 周边：Sidebar 切换会先乐观更新父状态并自行保存，设置页 `onChange` 也自行保存，父级同时负责刷新列表；`LibraryViewModel.saveLibrarySettings` 还残留未调用。后续新增设置项时很容易出现重复保存、失败回滚缺失或某个入口只更新 UI 不落库。 | 抽出 `LibrarySettingsController`/用例或让 `LibraryViewModel` 成为唯一设置写入口；所有 UI 只发意图，统一做乐观更新、持久化、失败提示和必要的列表刷新。删除未使用的 `saveLibrarySettings`，或把入口统一接回它。 | 待处理 |
| R-036 | Presentation / Reader Persistence | `Yomink/Presentation/Reader/ReaderHostView.swift:2737`, `Yomink/Presentation/Reader/ReaderHostView.swift:2752`, `Yomink/Presentation/Reader/ReaderHostView.swift:2753`, `Yomink/Presentation/Reader/ReaderHostView.swift:2758`, `Yomink/Presentation/Reader/ReaderHostView.swift:2765`, `Yomink/Presentation/Reader/ReaderHostView.swift:2769`, `Yomink/Presentation/Reader/ReaderHostView.swift:2774`, `Yomink/Presentation/Reader/ReaderHostView.swift:2778`, `Yomink/Presentation/Reader/ReaderHostView.swift:2791`, `Yomink/Presentation/Reader/ReaderHostView.swift:2792` | 建议修 | 阅读进度和阅读设置保存失败被静默吞掉：节流保存 catch 空实现，立即保存使用 `try?`。如果数据库写入失败、App 退后台时任务被取消或设置 JSON 编码失败，用户不会收到任何提示，下一次打开可能回到旧位置或旧设置。 | 至少对失败写 OSLog，并在连续失败或退出前最终保存失败时给 UI 提示；将“关闭阅读器前保存”改成可 await 的收尾流程，或维护 dirty 状态并在下次启动补偿。 | 待处理 |
| R-037 | Presentation / Import Flow | `Yomink/Presentation/Library/LibraryDedicatedPages.swift:2951`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:2993`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:3013`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:3110`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:3121`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:3123`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:3129`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:3142` | 建议修 | `ImportBookEditPage.startImport` 用无句柄 `Task` 执行重复检测和导入，页面取消/返回时不会取消正在运行的任务；任务完成后仍可能回调 `onImported`、`onOpenExistingBook` 或写 `errorMessage`。大文件导入期间用户离开页面，后台任务还会继续占用 I/O/内存并可能改变书库状态。 | 已给导入确认页增加 `importTask` 句柄，取消和 `onDisappear` 会 cancel；导入完成回写 UI 前检查 cancellation，取消后不触发导航回调或错误提示。 | 待验证 |
| R-038 | Presentation / Reader Mutations | `Yomink/Presentation/Reader/ReaderHostView.swift:3513`, `Yomink/Presentation/Reader/ReaderHostView.swift:3519`, `Yomink/Presentation/Reader/ReaderHostView.swift:3521`, `Yomink/Presentation/Reader/ReaderHostView.swift:3526`, `Yomink/Presentation/Reader/ReaderHostView.swift:3535`, `Yomink/Presentation/Reader/ReaderHostView.swift:3538`, `Yomink/Presentation/Reader/ReaderHostView.swift:3549`, `Yomink/Presentation/Reader/ReaderHostView.swift:5181`, `Yomink/Presentation/Reader/ReaderHostView.swift:5188` | 建议修 | 书签增删是乐观 UI 更新且持久化失败被 `try?` 吞掉。删除失败时 UI 已移除书签但数据库仍保留，新增失败时按钮恢复但没有错误提示；目录页删除同样不回滚。用户会在刷新或重进阅读器后看到书签“复活”或新增丢失。 | 将书签 mutation 抽成可回滚的操作：失败时恢复 UI 状态并提示；至少记录 OSLog。目录页和顶部按钮复用同一 mutation helper，避免两套乐观更新策略。 | 待处理 |

## 可选优化

| 问题编号 | 模块 | 文件行号 | 严重程度 | 问题 | 建议处理方式 | 当前状态 |
| --- | --- | --- | --- | --- | --- | --- |
| R-012 | Docs | `README.md:7`, `README.md:11`, `docs/PROJECT_DEVELOPMENT_PLAN.md:633`, `docs/PROJECT_DEVELOPMENT_PLAN.md:725` | 可选优化 | README 仍描述 Phase 0 和 reader placeholder，但源码已包含导入、书架、阅读器、搜索、存储等功能；开发计划中也保留阶段性暂缓/TODO 记录。文档和现状不一致会误导后续 review。 | 更新 README 的当前功能与验证方式；把开发计划中的历史暂缓项迁移成 issue 或本文件条目。 | 待处理 |
| R-013 | Localization | `Yomink.xcodeproj/project.pbxproj:412`, `Yomink.xcodeproj/project.pbxproj:474`, `Yomink/App/Resources/zh-Hans.lproj/Localizable.strings:1` | 可选优化 | Xcode development language 是 `en`，但仓库只有 `zh-Hans` 字符串。非中文系统或缺失 key 时可能直接显示 key。 | 若产品只面向中文，可考虑把开发语言调整为中文策略并明确；否则补 Base/en `Localizable.strings`，并增加缺失 key 检查。 | 待处理 |
| R-014 | Database / Query Performance | `Yomink/Data/Database/AppDatabase.swift:56`, `Yomink/Data/Database/AppDatabase.swift:89`, `Yomink/Data/Database/AppDatabase.swift:147`, `Yomink/Data/Database/AppDatabase.swift:180` | 可选优化 | 常用查询字段索引不完整：按分组筛书依赖 `books.groupId`，内容 hash 只是普通 index，搜索历史 keyword 区分大小写。当前数据量小问题不明显，书库变大后会影响性能或去重一致性。 | 视产品规则增加 `books(groupId, importedAt/lastReadAt)` 组合索引；评估 `contentHash` 是否应唯一；搜索历史按规范化 keyword 存储。 | 待处理 |
| R-015 | Cleanup / Temporary Files | `Yomink/Presentation/Library/LibraryView.swift:1321`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:2894`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:2549` | 可选优化 | `YominkExports` 临时导出目录只在下一次导出前清理，分享结束后不主动清理；存储页会把它计入临时导出占用。 | 在 `UIActivityViewController` completion 后清理，或 App 启动/进入后台时做一次安全清理。 | 待处理 |
| R-019 | App / DI | `Yomink/App/AppServices.swift:3`, `Yomink/App/AppServices.swift:5`, `Yomink/App/AppEnvironment.swift:27`, `Yomink/App/AppEnvironment.swift:62` | 可选优化 | `AppServices` 暴露 `database`，但当前调用方只使用 `fileStore`、`libraryRepository`、`importService`；`GRDBLibraryRepository` 已经持有 `AppDatabase`，因此这个字段更多是未使用装配泄漏。 | 如果没有 UI 或调试入口需要直接访问数据库，从 `AppServices` 删除 `database` 字段；若保留，应注释说明它是为了生命周期持有或后续诊断入口。 | 待处理 |
| R-020 | App / Localization Cleanup | `Yomink/App/Resources/zh-Hans.lproj/Localizable.strings:145`, `Yomink/App/Resources/zh-Hans.lproj/Localizable.strings:146`, `Yomink/App/Resources/zh-Hans.lproj/Localizable.strings:147` | 可选优化 | 本地化资源里仍保留早期 reader placeholder 文案，静态检索未发现生产代码引用；同类孤儿 key 还包括 `library.title`、`library.count`、`add.title` 等。资源文件会逐渐变成历史状态混杂处，增加翻译和缺失 key 审查成本。 | 增加本地化 key 使用检查脚本；删除确认无引用的 placeholder/历史 key，或把仍计划使用的 key 标注到对应任务。与 R-013 的 Base/en 语言策略一起处理更省力。 | 待处理 |
| R-025 | Domain / Preview Support | `Yomink/Domain/Services/PreviewLibraryRepository.swift:3`, `Yomink/Domain/Services/PreviewLibraryRepository.swift:21`, `Yomink/Domain/Services/PreviewLibraryRepository.swift:54`, `Yomink/Domain/Services/PreviewLibraryRepository.swift:62`, `Yomink/Domain/Services/PreviewLibraryRepository.swift:121`, `Yomink/Domain/Services/PreviewLibraryRepository.swift:173`, `Yomink/Domain/Services/PreviewLibraryRepository.swift:182`, `Yomink/Domain/Services/PreviewLibraryRepository.swift:219` | 可选优化 | `PreviewLibraryRepository` 位于 Domain 生产目录，却是 SwiftUI preview/test support 性质的假仓储；实现是无状态的，`fetchBooks` 每次生成新的 `Date()`，保存设置、进度、书签、分组等方法大多 no-op 或返回临时对象。预览里修改 UI 状态后再次读取不会体现真实仓储行为，容易掩盖交互问题。 | 将它移到 PreviewSupport/TestSupport 或 App preview 装配目录；若继续作为 UI 预览依赖，建议改成 actor/内存仓储，至少让 create/update/save/delete 在一次预览会话内可观察。 | 待处理 |
| R-026 | Domain / Unused Code | `Yomink/Domain/Services/ImportService.swift:10`, `Yomink/Domain/Services/ImportService.swift:17`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:3131` | 可选优化 | `ImportBookPreview.metadata` 计算属性未被调用；确认导入时 Presentation 自己重新组装 `ImportBookMetadata`。这类 convenience API 留在 Domain 会制造“推荐用法”的误导，也增加后续字段调整时的同步成本。 | 若没有计划让 UI 直接使用 `preview.metadata`，删除该计算属性；或反过来让导入确认路径复用它，避免 metadata 组装逻辑分散。 | 待处理 |
| R-032 | Data / Schema Cleanup | `Yomink/Data/Database/AppDatabase.swift:68`, `Yomink/Data/Database/AppDatabase.swift:69`, `Yomink/Data/Database/Records/BookRecord.swift:19`, `Yomink/Data/Database/Records/BookRecord.swift:20`, `Yomink/Data/Database/GRDBLibraryRepository.swift:806`, `Yomink/Data/Database/GRDBLibraryRepository.swift:807`, `docs/PROJECT_DEVELOPMENT_PLAN.md:123`, `docs/PROJECT_DEVELOPMENT_PLAN.md:124` | 可选优化 | `books.importSourceDisplayPath` 和 `books.sourceBookmark` 是计划中的来源追踪字段，但当前生产代码只写入展示路径、从不读取；`sourceBookmark` 始终写 nil。它们会增加 schema 和 record 映射噪音，也容易让后续误以为支持重新访问外部原始文件。 | 若近期不做原始文件重访，删除 `sourceBookmark` 相关记录字段或在文档中标注为预留；`importSourceDisplayPath` 若要保留，应接入存储详情展示或导入记录，否则一并清理。 | 待处理 |
| R-033 | Data / API Boundary | `Yomink/Data/Database/GRDBLibraryRepository.swift:559`, `Yomink/Data/Database/GRDBLibraryRepository.swift:586`, `Yomink/Data/Database/GRDBLibraryRepository.swift:588`, `Yomink/Domain/Services/PreviewLibraryRepository.swift:184`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:500` | 可选优化 | `fetchReadingHistory(limit:)` 对小于 1 的 limit 使用 `max(limit, 1)`，调用方传 0 本应表达不取数据时却会返回 1 条；preview fake 则直接 `prefix(limit)`，同一协议在不同实现上的边界行为不一致。当前 UI 只传 30，暂未形成用户可见 bug。 | 明确协议语义：`limit <= 0` 返回空数组或抛出参数错误；真实仓储和 preview fake 保持一致，并加一个边界测试。 | 待处理 |
| R-039 | Presentation / Storage Structure | `Yomink/Presentation/Library/LibraryDedicatedPages.swift:1766`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:1848`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:2526`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:2532`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:2602`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:2877`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:2894` | 可选优化 | 存储页在 Presentation 内直接扫描 Documents/ApplicationSupport、计算数据库/WAL/SHM 大小、统计目录、导出文件。它更像 storage diagnostics/export 的应用服务而不是视图职责；当前实现让 UI 文件继续膨胀，也让后续测试只能绕 UI 类型做。 | 将 `StorageUsageScanner` 和 `StorageBookExporter` 移到 Data/Application 支持层，Presentation 只订阅快照和发起导出动作。这样也能和 R-008 的统一导出服务合并处理。 | 待处理 |
| R-040 | Presentation / Unused Code | `Yomink/Presentation/Library/LibraryView.swift:1385`, `Yomink/Presentation/Library/LibraryView.swift:1391`, `Yomink/Presentation/Library/LibraryView.swift:1393`, `Yomink/Presentation/Library/LibraryView.swift:1395`, `Yomink/Presentation/Library/LibraryView.swift:538`, `Yomink/Presentation/Library/LibraryView.swift:586`, `Yomink/Presentation/Library/LibraryView.swift:1426` | 可选优化 | `LibraryViewModel.saveLibrarySettings(repository:)` 静态检索未发现调用；`BookShelfItemButton` 初始化参数接收 `book` 但内部完全不使用。它们都是历史实现残留，会干扰后续判断“设置保存入口”和书架按钮职责。 | 删除未调用方法和无用参数；如果保留是为未来入口，补注释并接入实际调用。 | 待处理 |
| R-041 | Presentation / Formatting Duplication | `Yomink/Presentation/Library/LibraryView.swift:1697`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:2869`, `Yomink/Presentation/Library/LibraryDedicatedPages.swift:3921`, `Yomink/Presentation/Reader/ReaderHostView.swift:4222`, `Yomink/Presentation/Reader/ReaderHostView.swift:4231` | 可选优化 | 阅读进度百分比格式化在书架、存储页、Reader 多处各自定义 `NumberFormatter`，clamp 规则和小数位需要人工保持一致。现在行为基本接近，但后续调整百分比精度或本地化时容易漏改。 | 抽出轻量 `ProgressFormatters` 或 `ReadingProgressFormatter`，统一 percent formatter 和 clamp 规则；顺手合并存储页的专用 formatter。 | 待处理 |

## 已检查但未发现明显问题

- 未发现 `print`、`debugPrint`、`dump` 等残留调试输出。
- 当前依赖只有 GRDB SwiftPM pin，未发现广告、统计、崩溃上报或网络 SDK。
- `Info.plist` 未配置网络权限或 ATS 例外；源码中未发现 `URLSession` 调用。
- `fatalError("init(coder:) has not been implemented")` 只出现在显式标记 unavailable 的 UIKit 初始化器中，当前不作为问题记录。

## 本次验证限制

- 当前环境是 Windows，未安装 `xcodebuild` / `swift`，无法本地执行 iOS 编译、类型检查、XCTest 或 Instruments。
- Git 仓库存在 ownership safe.directory 限制，本次只用 `git -c safe.directory=E:/GithubRepo/Yomink-ios-app status --short` 做只读状态确认。
